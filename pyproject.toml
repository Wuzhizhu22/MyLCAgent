[project]
name = "MyLCAgent"
version = "0.2.0"
description = "使用LangChain框架构建的简易智能体，支持工具调用和结构化响应"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain>=1.2.0",
    "langchain-deepseek>=1.0.1",
]
